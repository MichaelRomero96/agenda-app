import { createStyles } from '@mantine/core'

const useStyles = createStyles(() => ({
  root: {
    backgroundColor: 'red',
  },
  addTaskContainer: {
    margin: '0 auto',
  },
}))

export default useStyles
